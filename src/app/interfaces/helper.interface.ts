// todo: 1. send email verification 2.vaalidate email && Password
// todo: 3. for the module notification it needs its own service
export interface IAuthHelper {
    // sendEmailVerification(email: string):Promise<boolean>;
}
